# Portable Python dependencies used by vehicle_server.py.
#
# Install these into the Jetson's existing JetPack Python environment. PyTorch
# and OpenCV are intentionally omitted: use NVIDIA's JetPack-compatible builds
# instead of replacing them with generic PyPI wheels. The Hikrobot MVS SDK
# (MvCameraControl_class) and the local fire.pt model are also user-provided.
numpy>=1.24,<3
pyserial>=3.5,<4
smbus2>=0.4,<1
ultralytics>=8.3,<9
websockets>=12,<16
